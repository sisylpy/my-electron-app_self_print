# 桌面调度写操作权限与接口设计方案

> 项目：农心乐配送商系统  
> Electron 项目：`ai-priter-electic`  
> 项目路径：`/Users/lpy/Documents/javaWeb/imJava/nongxinle/app/nxl-distributer/ai-priter-electic`  
> 业务后端：`nongxinle-server`  
> 参考端：`boss-mini`  
> 方案日期：2026-07-30  
> 当前阶段：Slice 2 前置设计  
> 本文只做权限、接口和开发切片设计，不修改 Electron 或 `nongxinle-server` 代码

## 1. 结论

Electron 最终可以发起以下七类调度操作：

1. 分配司机；
2. 调整路线；
3. 修改站点顺序；
4. 确认派单；
5. 装车确认；
6. 发车确认；
7. 异常处理。

但它们现在不能直接全部开放。

当前服务端已有分配、路线编辑、确认路线、发车、送达和退回沙盘等业务服务，
可以继续作为业务主权；Electron 不需要也不能复制派单规则。上线写操作前，必须先在
`nongxinle-server` 建立统一的可信身份、配送商数据隔离、细粒度权限、幂等、并发控制
和不可变审计。

当前最关键的上线阻断项如下：

- `spring-shiro.xml` 将 `/api/**` 配置为匿名访问；
- `TodayDispatchController` 没有方法级权限校验；
- `disId`、`operatorUserId` 由客户端请求体直接提交；
- Boss 小程序从本地缓存取得这两个字段，不能作为可信身份；
- 现有配送商用户类型是粗粒度岗位类型，不足以表达调度写权限；
- 路线和派送任务没有可用于乐观锁的版本字段；
- 现有写接口没有持久化幂等键；
- 现有操作人、原因、时间字段只是业务表上的局部记录，不等于完整审计日志；
- “装车确认”没有独立正式写接口；
- Boss 端 `MARK_DELIVERY_EXCEPTION` 仍为“功能未接入”，没有完整异常处理闭环。

因此 Slice 2 的正确顺序是：

```text
服务端可信身份与权限
  → 幂等、并发控制和审计
  → 服务端动作能力合同
  → Electron 写请求基础设施
  → 分配与路线写操作
  → 发车
  → 装车显式确认（如业务确认需要）
  → 异常登记与处置
```

写按钮未满足对应服务端门槛时必须保持隐藏或禁用，不能先在 Electron 放开后依靠
客户端提示控制风险。

## 2. 不可改变的业务主权

### 2.1 `nongxinle-server` 决定

- 当前用户是否属于目标配送商；
- 当前用户是否拥有本次操作权限；
- 客户订单是否仍然有效、可派；
- 司机是否属于当前配送商、是否当班、是否可接单；
- 司机当前阶段是否允许插入或调整站点；
- 候选站点顺序是否完整、合法；
- 路线距离、时间、时间窗、可行性和风险；
- 是否允许确认派单；
- 是否满足装车或发车条件；
- 路线和派送任务能否推进到下一状态；
- 异常类型、允许处置方式和处置后的状态；
- 并发冲突后是否需要重新加载、重新试算。

### 2.2 Electron 只负责

- 展示服务端返回的数据和可用动作；
- 采集用户选择、候选司机、候选站点顺序、原因和备注；
- 将候选方案提交服务端预览；
- 展示服务端试算结果、风险和禁用原因；
- 在用户确认后提交服务端命令；
- 展示命令结果并重新拉取权威读模型；
- 防止界面重复点击，但不把前端防重复当作幂等保障。

### 2.3 Electron 禁止

- 根据本地司机状态自行放开派单；
- 在本地计算并认可路线合法性；
- 直接把拖拽结果当作正式路线；
- 本地修改阶段后等待后台同步；
- 伪造 `operatorUserId` 或切换 `disId`；
- 把打印完成自行等同为实际装车完成；
- 通过 Electron IPC、MCP 或本地文件绕过业务 API；
- 执行服务端响应中任意 URL。

服务端可以返回 `actionType`、`enabled`、`disabledReason`、`confirmMessage`、
`expectedVersion` 和一次性 `previewToken`。Electron 只能把已知 `actionType`
映射到本地白名单 API 方法，不能像通用浏览器一样直接请求服务端下发的任意路径。

## 3. 现有接口与可开放状态

| 操作 | 当前服务端能力 | 当前可直接开放给 Electron | 结论 |
|---|---|---:|---|
| 分配司机 | `POST /sandbox/stops/confirm`；司机全景和路线编辑预览 | 否 | 服务可复用，接口必须先加鉴权、租户校验、幂等和版本 |
| 调整路线 | route edit `page → preview → confirm` | 否 | 继续服务端试算；正式确认需防并发 |
| 修改站点顺序 | route edit `preview → confirm` | 否 | 不做逐次拖拽写入，整条候选顺序原子确认 |
| 确认派单 | `POST /sandbox/driver-route-edit/confirm` | 否 | 当前确认后会直接进入装车，桌面必须准确展示该结果 |
| 装车确认 | 无独立正式命令；打印成功可使任务进入 `READY_TO_GO` | 否 | 先确认业务语义，再由服务端补接口；Electron 不造状态 |
| 发车确认 | `POST /drivers/{driverUserId}/depart-now` | 否 | 服务存在，但当前校验不足，必须接入现有发车策略后开放 |
| 异常处理 | 送达、退回沙盘有命令；通用异常登记未接入 | 否 | 送达/退回不能冒充异常中心，需新增异常登记和处置合同 |

本文建议为桌面写操作增加受保护的版本化命令门面，例如：

```text
/api/desktop-dispatch/v1/**
```

该门面位于 `nongxinle-server`，只负责：

- 认证和权限；
- 配送商归属校验；
- 外部 DTO 收口；
- 幂等和乐观锁；
- 审计；
- 调用现有 Today Dispatch 业务服务。

它不是第二套派单逻辑。这样可以避免 Electron 直接依赖现有 DTO 中的内部字段，
也能在不立即破坏 Boss 小程序的前提下逐步收紧旧接口。

## 4. 角色与权限模型

### 4.1 当前角色现状

当前 `nxDiuAdmin` 的既有类型为：

| 值 | 现有含义 |
|---:|---|
| `0` | 管理员 |
| `1` | 文员 |
| `2` | 库房管理员 |
| `4` | 采购员 |
| `5` | 司机 |

该字段更接近岗位类型，不足以单独承担商业产品级授权。例如“文员”是否可以发车、
“库房管理员”是否可以调整路线，都不能由客户端推断。

### 4.2 目标授权方式

服务端应返回权限集合，角色只作为权限分组：

| 权限 | 含义 |
|---|---|
| `dispatch:assignment:write` | 分配或改派司机 |
| `dispatch:route:edit` | 调整路线和站点顺序 |
| `dispatch:confirm` | 确认派单、进入服务端规定的下一阶段 |
| `dispatch:loading:confirm` | 确认装车完成 |
| `dispatch:depart` | 确认发车 |
| `dispatch:exception:report` | 登记异常 |
| `dispatch:exception:resolve` | 决定异常处置 |
| `dispatch:audit:read` | 查询操作审计 |

推荐角色矩阵：

| 角色 | 分配司机 | 路线/顺序 | 确认派单 | 装车确认 | 发车 | 报告异常 | 处置异常 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 配送商管理员 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| 调度员 | 是 | 是 | 是 | 否 | 是 | 是 | 按授权 |
| 库房/装车员 | 否 | 否 | 否 | 是 | 按授权 | 装车异常 | 否 |
| 司机 | 否 | 否 | 否 | 自身且按授权 | 自身且按授权 | 自身任务 | 否 |
| 采购员/只读人员 | 否 | 否 | 否 | 否 | 否 | 否 | 否 |

首轮生产灰度建议只给“配送商管理员”开放桌面写权限。调度员、库房员扩权必须由
服务端显式配置，不能把全部 `nxDiuAdmin=1/2` 用户自动升级为写操作人员。

司机的“自身任务”约束必须由服务端把登录人和 `driverUserId` 绑定。桌面老板工作台
首版不建议以司机角色登录执行司机自助操作。

### 4.3 三层校验

每次写命令至少经过三层校验：

1. **认证过滤层**：验证访问令牌、设备/会话状态、令牌过期和撤销；
2. **授权与租户层**：检查权限，并确认目标司机、路线、站点、任务都属于登录人的
   `disId`；
3. **领域服务层**：重新校验司机可用性、路线合法性、任务状态和状态机迁移。

Electron 路由守卫和按钮权限只提供界面体验，不属于安全边界。

## 5. 七类操作详细设计

### 5.1 分配司机

**是否允许 Electron 发起：**允许，但必须经过服务端候选查询/试算，并在服务端安全
基线完成后开放。

**操作角色：**

- 配送商管理员；
- 拥有 `dispatch:assignment:write` 的调度员。

**当前可复用能力：**

- 候选司机：`POST /sandbox/manual-dispatch/driver-panorama`；
- 路线编辑页/试算：`POST /sandbox/driver-route-edit/page`、
  `POST /sandbox/driver-route-edit/preview`；
- 单站确认分配：`POST /sandbox/stops/confirm`。

**建议正式桌面接口：**

```text
POST /api/desktop-dispatch/v1/stops/{stopId}/assignment-preview
POST /api/desktop-dispatch/v1/stops/{stopId}/assign
```

预览请求只提交候选 `driverUserId`、候选顺序和可选原因。`disId`、`operatorUserId`
由服务端会话取得。确认请求携带服务端预览生成的 `previewToken` 和
`expectedRouteVersion`。

**服务端校验：**

- 站点和有效订单仍存在且属于当前配送商；
- 司机属于当前配送商且处于服务端允许的当班/阶段；
- 站点未被其他司机执行或完成；
- 追加到装车中或配送中路线时，必须按服务端规则返回风险确认；
- 路线、时间窗和业务状态仍满足确认条件；
- 预览版本没有过期。

**幂等：**

- 必须携带 `Idempotency-Key`；
- 同一键、同一请求摘要返回第一次结果；
- 同一键、不同请求摘要返回 `409 IDEMPOTENCY_CONFLICT`；
- 现有 `nx_dst_open_key` 只能防止部分重复开放任务，不能代替命令幂等。

**审计：**

- 记录站点、原司机、新司机、原路线/新路线、原因、预览版本、风险确认内容；
- 记录操作人、配送商、设备、客户端版本、请求 ID、结果和错误码；
- 不在日志中保存完整商品明细、访问令牌或个人敏感信息。

### 5.2 调整路线

**是否允许 Electron 发起：**允许。客户端只能提交候选方案，服务端负责试算和确认。

**操作角色：**

- 配送商管理员；
- 拥有 `dispatch:route:edit` 的调度员。

**当前可复用能力：**

```text
POST /sandbox/driver-route-edit/page
POST /sandbox/driver-route-edit/preview
POST /sandbox/driver-route-edit/confirm
```

**建议正式桌面接口：**

```text
POST /api/desktop-dispatch/v1/route-edits/preview
POST /api/desktop-dispatch/v1/dispatch-confirmations
```

新司机路线在确认前可能还没有 `routeId`，因此 preview 以 `driverUserId`、候选站点和
服务端快照为输入；已有路线同时携带 `routeId` 和版本。正式写入统一由
“确认派单”命令消费 preview token，避免为新路线伪造 ID。

**服务端校验：**

- 路线、司机和所有站点属于当前配送商；
- 请求站点集合无重复、无越权添加、无静默丢失；
- 已配送/关闭站点不能被移动；
- 已出发路线是否允许追加或调整，由服务端规则决定；
- 司机当班状态、路线阶段和可行性有效；
- 重新计算距离、耗时、时间窗、迟到风险和预计送达；
- 确认时重新验证，而不是只信任先前 preview。

**幂等与并发：**

- preview 本身不改变业务状态，可用 `X-Request-Id` 追踪，不强制业务幂等；
- confirm 必须使用 `Idempotency-Key`；
- preview 返回短时有效、一次性或可重放的 `previewToken`；
- token 绑定配送商、路线、司机、站点集合、顺序、基础版本和到期时间；
- 已有路线 confirm 使用数据库条件更新 `WHERE route_id=? AND version=?`；
- 新路线 confirm 校验派单计划/业务快照版本，并依赖数据库唯一约束防止并发重复创建；
- 冲突返回 `409 ROUTE_VERSION_CONFLICT`，Electron 重新读取并要求用户重新试算。

**审计：**

- 记录调整前后站点 ID 和顺序；
- 记录试算摘要、风险确认、理由和版本；
- 不把每次拖拽写成业务审计；只记录正式确认和必要的失败/拒绝。

### 5.3 修改站点顺序

**是否允许 Electron 发起：**允许，但它属于“路线调整”的一个子命令。

**操作角色、权限和接口：**与调整路线相同。

**交互原则：**

- 拖拽、上移、下移只修改 Electron 内存中的候选顺序；
- 页面明确标记“尚未试算”；
- 用户停止编辑后主动点击试算，或由前端做防抖 preview；
- 未取得服务端最新 preview，不允许正式确认；
- 不为每一次拖拽调用写接口；
- 正式保存时一次提交完整 `stopIds/stopKeys` 顺序，由服务端原子确认。

**幂等与审计：**与路线确认使用同一个命令，不另造逐站点幂等记录。

### 5.4 确认派单

**是否允许 Electron 发起：**允许，是高风险状态命令。

**操作角色：**

- 配送商管理员；
- 拥有 `dispatch:confirm` 的调度员。

**当前可复用能力：**

```text
POST /sandbox/driver-route-edit/confirm
```

当前实现的关键语义是：确认路线后会写入司机路线，并进入 `LOADING`，响应下一页为
装车页。因此 Electron 不能把它展示成“只保存草稿”。

**建议正式桌面接口：**

```text
POST /api/desktop-dispatch/v1/dispatch-confirmations
```

该接口内部调用现有路线确认服务，消费 `previewToken`。它既支持修改已有路线，也支持
服务端在确认时创建新司机路线，并返回服务端真实状态：

```json
{
  "operationId": "server-generated",
  "replayed": false,
  "routeId": 1001,
  "routeStatus": "LOADING",
  "routeVersion": 8,
  "nextStage": "LOADING"
}
```

**服务端校验：**

- 权限、租户、路线和司机归属；
- preview token、版本和候选内容；
- 有效订单快照；
- 当前站点不存在被其他司机执行的冲突；
- 路线合法性和服务端 `confirmEnabled`；
- 状态机允许从当前状态进入服务端决定的下一状态。

**幂等：**

- 强制 `Idempotency-Key`；
- 网络超时重试不得重复创建路线、重复改写确认时间或重复进入装车；
- 回放返回第一次成功的 `operationId`、状态和版本。

**审计：**

- 保存确认前后路线状态、司机、站点集合、版本、风险确认和确认理由；
- 业务数据和成功审计应在同一事务提交；
- 权限拒绝、规则拒绝和系统失败也要留下安全审计，但不能因审计失败反向放行业务。

### 5.5 装车确认

**是否允许 Electron 发起：**条件允许；当前版本暂不开放。

**现状：**

- 当前路线确认会进入“装车中”；
- 当前没有独立的“装车完成/装车确认”正式 API；
- `DisShipmentTaskService.onBillPrinted` 可在配送单打印成功后把任务推进为
  `READY_TO_GO`；
- 当前配置 `require-load-before-depart` 默认允许不逐站确认装车；
- 因此“打印完成”“进入装车”和“物理装车完成”不是同一个概念。

**第一阶段兼容策略：**

- Electron 只读展示服务端装车状态；
- 不新增本地“装车完成”状态；
- 不把打印按钮成功当作整条路线装车确认；
- 发车能否执行仍由服务端发车策略决定。

**如果业务确认必须增加显式装车完成：**

```text
POST /api/desktop-dispatch/v1/routes/{routeId}/loading-confirm
```

**操作角色：**

- 配送商管理员；
- 拥有 `dispatch:loading:confirm` 的库房/装车员；
- 调度员仅在被显式授权时可操作。

**服务端校验：**

- 路线处于允许装车确认的阶段；
- 路线仍属于当前配送商；
- 所有待送站点、订单、配送单和装车条件满足；
- 具体是“逐站装车”还是“整车完成”，必须由后端合同统一定义；
- 状态推进到 `READY_TO_DEPART` 或现有兼容状态由服务端决定。

**幂等与审计：**

- 路线级 `Idempotency-Key` 和 `expectedRouteVersion`；
- 重复请求不重写完成时间；
- 记录确认人、路线、站点/单据摘要、前后状态和异常说明。

在服务端没有明确合同前，Slice 2 不实现该按钮。

### 5.6 发车确认

**是否允许 Electron 发起：**允许，但现有接口必须先加固。

**操作角色：**

- 配送商管理员；
- 拥有 `dispatch:depart` 的调度员；
- 库房人员仅在业务明确授权时可发车；
- 司机自助发车必须限定为登录司机本人，不属于桌面老板工作台首版。

**当前接口：**

```text
POST /drivers/{driverUserId}/depart-now
```

当前 `departNow` 服务会直接把路线和任务改为 `IN_DELIVERY`，而且在找不到路线时可能
创建路线；它没有完整复用已经存在的 `DisRouteDriverDepartPolicy.evaluateDepart`。
因此不能原样开放。

**建议正式桌面接口：**

```text
POST /api/desktop-dispatch/v1/routes/{routeId}/depart
```

请求不再以客户端提交的 `driverUserId` 作为唯一目标，服务端从路线实体解析司机。

**服务端校验：**

- 路线和司机属于当前配送商；
- 司机当班；
- 路线存在且不能在发车命令中临时创建；
- 路线不处于配送中、已完成或已关闭；
- 存在已确认的可配送站点，没有未确认站点；
- 装车要求按服务端配置满足；
- 路线可行性不是 `INFEASIBLE`；
- 当前版本一致；
- 调用统一发车策略后才允许改变状态。

**幂等：**

- 强制 `Idempotency-Key`；
- 第一次成功生成的 `actualDepartAt` 必须保持不变；
- 同一路线已经发车时，相同键返回原结果，不同新键返回明确的已执行/状态冲突，
  不能再次改写出发时间。

**审计：**

- 路线、司机、前后状态、真实出发时间、未打印单据警告、操作备注；
- 操作人从服务端身份取得，不能信任请求体；
- 发车后任何禁止改派/退回的规则拒绝也应有审计。

### 5.7 异常处理

**是否允许 Electron 发起：**允许，但必须先新增完整服务端合同。

**现状：**

- 已有“确认送达”和“退回沙盘”命令；
- 任务实体已经有异常类型、异常说明和异常时间字段；
- Boss 配送页的 `MARK_DELIVERY_EXCEPTION` 仍提示“功能未接入”；
- 送达或退回沙盘是异常的处置结果之一，不等于异常登记本身。

**建议拆成两步：**

```text
POST /api/desktop-dispatch/v1/delivery/stops/{stopId}/exceptions
POST /api/desktop-dispatch/v1/delivery/stops/{stopId}/exceptions/{exceptionId}/resolve
```

登记异常请求包含：

- 服务端枚举 `exceptionType`；
- 必填原因和可选备注；
- `occurredAt` 只作现场时间参考，服务端另存接收时间；
- 可选凭证 ID，文件上传走独立受控接口；
- `expectedTaskVersion`。

处置动作由服务端返回允许集合，例如：

```text
RETRY_DELIVERY
RETURN_TO_SANDBOX
MARK_DELIVERED
CANCEL
```

Electron 不自行决定某种异常对应哪个状态。

**操作角色：**

- 配送商管理员：登记和处置；
- 调度员：登记，按权限处置；
- 司机：只能登记自己的当前任务异常；
- 库房员：只能登记装车阶段异常，不能处置配送异常。

**服务端校验：**

- 站点、任务、路线和司机归属；
- 当前任务状态是否允许登记异常；
- 异常类型是否适用于当前阶段；
- 当前用户能否操作该任务；
- 处置动作是否在服务端允许集合内；
- 处置后的状态迁移、路线释放和沙盘回流规则。

**幂等：**

- 登记和处置分别使用 `Idempotency-Key`；
- 防止同一现场异常重复生成多个活动异常；
- 处置命令必须使用异常版本和任务版本；
- 已关闭异常不能再次处置。

**审计：**

- 异常类型、原因、任务/路线/司机、证据 ID、登记人和处置人；
- 记录前后状态与处置结果；
- 修改原因只能追加补充，不覆盖原始异常记录。

## 6. 统一认证与会话设计

### 6.1 服务端可信身份

服务端为桌面端建立 `DispatchPrincipal`，至少包含：

```text
actorUserId
disId
roles
permissions
sessionId
deviceId
```

Controller 和 Service 从该身份取得操作人和配送商。外部请求即使携带
`operatorUserId` 或 `disId`，也必须忽略或与 principal 严格比对，不能据此授权。

### 6.2 Electron 凭证边界

- 使用服务端签发的短期访问令牌；
- 访问令牌只保存在运行内存；
- 长期刷新凭证如必须存在，由 Electron 主进程通过系统安全凭证存储保管；
- 渲染进程不把登录凭证写入 `localStorage`、普通 JSON 配置或 Vuex 持久化；
- preload 只暴露固定的会话方法，不暴露任意文件、网络或系统命令能力；
- 所有调度业务仍通过 HTTPS 调用 `nongxinle-server`，不经 MCP 打印服务；
- 注销、账号禁用、设备撤销后服务端令牌立即失效。

### 6.3 旧接口迁移

不能只给请求增加 `X-Client-Type: electron` 或来源字符串，这些字段可以伪造。

建议迁移顺序：

1. 新建受保护桌面命令门面；
2. Electron 只调用新门面；
3. Boss 小程序逐步接入同一身份和权限体系；
4. 旧匿名写接口完成调用方迁移后关闭；
5. 最终将 `/api/nxdisroutedispatch/**` 从匿名白名单移出。

## 7. 统一写接口合同

### 7.1 请求头

```http
Authorization: Bearer <short-lived-access-token>
Idempotency-Key: <uuid>
X-Request-Id: <uuid>
X-Client-Version: <electron-version>
If-Match: "<aggregate-version>"
```

- `Authorization`、`Idempotency-Key` 为写命令必填；
- `If-Match` 可与请求体 `expectedVersion` 二选一，项目内必须统一；
- 服务端生成最终 `operationId`，不信任客户端生成的审计 ID。

### 7.2 请求体边界

外部 DTO 只接受业务必要字段。以下内部字段不能由 Electron 提交：

- `suppressTodayResponse`；
- `deferPlanRefresh`；
- `planId` 的内部覆盖语义；
- `eligibleOrderSnapshotCache`；
- 任意服务端内部刷新或绕过校验开关。

`operatorUserId`、`disId` 不作为可信写字段。路由日期、批次也必须与目标聚合实体比对，
不能仅凭客户端传值查询和更新。

### 7.3 统一响应

```json
{
  "code": 0,
  "requestId": "request-id",
  "data": {
    "operationId": "operation-id",
    "replayed": false,
    "aggregateId": 1001,
    "aggregateVersion": 8,
    "status": "LOADING",
    "nextStage": "LOADING"
  }
}
```

写成功后 Electron 必须重新拉取 Slice 1 的权威读模型。命令响应可以帮助即时反馈，
但不取代工作台刷新。

### 7.4 错误合同

| HTTP | 错误码 | Electron 行为 |
|---:|---|---|
| 401 | `UNAUTHENTICATED` | 清理当前会话并要求重新登录 |
| 403 | `PERMISSION_DENIED` | 禁用动作并刷新权限 |
| 404 | `RESOURCE_NOT_FOUND` | 不泄露跨配送商资源是否存在 |
| 409 | `ROUTE_VERSION_CONFLICT` | 刷新路线，要求重新试算 |
| 409 | `IDEMPOTENCY_CONFLICT` | 停止自动重试，显示请求冲突 |
| 409 | `STATE_CHANGED` | 刷新工作台，展示服务端新状态 |
| 422 | `BUSINESS_RULE_REJECTED` | 展示服务端安全文案和禁用原因 |
| 429 | `TOO_MANY_REQUESTS` | 按服务端提示退避 |
| 500 | `INTERNAL_ERROR` | 显示请求 ID，不直接展示堆栈或 SQL 信息 |

现有 Controller 把异常消息直接包装返回的做法需要收口为稳定错误码和安全文案。

## 8. 幂等与并发控制

### 8.1 幂等记录

建议持久化命令幂等记录，唯一范围为：

```text
(disId, actorUserId, actionType, idempotencyKey)
```

至少保存：

- 请求摘要；
- 操作 ID；
- 目标聚合；
- 执行状态；
- 首次响应摘要；
- 创建和完成时间。

同键同摘要返回原结果；同键不同摘要拒绝。保留期至少覆盖现场网络重试和跨日补偿，
建议不少于 7 天。

### 8.2 乐观锁

路线和派送任务增加版本字段：

```text
route_version
task_version
```

所有确认型更新使用带版本的条件更新，并在成功后递增版本。仅靠
`updated_at`、本地刷新时间或页面上的站点数不能可靠防止两个调度员互相覆盖。

### 8.3 预览令牌

路线 preview 返回不透明的 `previewToken`，至少绑定：

- 配送商；
- 操作人；
- 路线和司机；
- 站点集合与顺序摘要；
- 路线基础版本；
- 业务规则版本；
- 过期时间。

confirm 仍需重新读取数据库并执行最终领域校验，preview token 不是绕过校验的凭证。

## 9. 审计设计

建议增加不可变调度操作日志，至少记录：

| 字段 | 说明 |
|---|---|
| `operation_id` | 服务端生成的全局操作 ID |
| `request_id` | 请求链路 ID |
| `idempotency_key` | 幂等键 |
| `source` | `ELECTRON_DESKTOP`、`BOSS_MINI` 等 |
| `dis_id` | 配送商 |
| `actor_user_id` | 真实操作人 |
| `actor_role_snapshot` | 当时角色/权限摘要 |
| `device_id`、`client_version` | 设备和版本 |
| `action_type` | 操作类型 |
| `target_type`、`target_id` | 路线、站点、任务或异常 |
| `before_status/version` | 操作前状态和版本 |
| `after_status/version` | 操作后状态和版本 |
| `change_summary` | 脱敏后的司机、站点和顺序变化摘要 |
| `reason` | 人工操作原因 |
| `result`、`error_code` | 成功、拒绝、失败 |
| `created_at` | 服务端时间 |

要求：

- 审计只能追加，普通业务接口不能修改或删除；
- 成功业务与成功审计尽量同事务；
- 权限拒绝、业务拒绝和系统失败单独记录安全审计；
- 不记录令牌、完整请求体、商品价格明细、手机号和地址全文；
- 管理员查询审计需要独立的 `dispatch:audit:read` 权限。

## 10. Electron 模块边界

Slice 2 写能力仍放在 `vue/src/modules/dispatch`，建议新增：

```text
vue/src/modules/dispatch/
├── api/
│   ├── dispatchReadApi.js
│   └── dispatchCommandApi.js
├── commands/
│   ├── commandRegistry.js
│   ├── idempotency.js
│   └── resultMapper.js
├── permissions/
│   └── dispatchCapabilities.js
├── services/
│   ├── routeDraft.js
│   └── dispatchRefresh.js
└── components/
    ├── RouteEditDialog.vue
    ├── DispatchConfirmDialog.vue
    └── ExceptionDialog.vue
```

边界要求：

- `dispatchCommandApi` 只包含固定白名单接口；
- 调度写入不进入 `print`、`order`、`map` 模块；
- route draft 只保存临时候选顺序，不保存业务真相；
- token、幂等键和失败重试由统一命令层处理；
- POST 超时后先按同一幂等键查询/重试，不能生成新键盲目重发；
- 动作成功统一刷新分派中、装车中、配送中和司机读模型；
- 当前已有轮询遇到编辑弹窗时不能覆盖用户草稿，应提示“服务端数据已变化”；
- 路线版本变化时丢弃旧 preview，要求重新试算；
- Electron 不通过 preload 新增调度业务 IPC；
- MCP、Bills、OCR 和打印队列不引用 dispatch command。

## 11. Slice 2 开发切片

### Slice 2.0：服务端写安全底座

**范围：**

- 桌面端认证与 `DispatchPrincipal`；
- 配送商归属校验；
- 权限定义和默认拒绝；
- 受保护的桌面命令门面；
- 统一错误码；
- 幂等记录、操作审计、路线/任务版本；
- 关闭外部 DTO 的内部控制字段。

**验收标准：**

- 未登录调用全部写接口返回 401；
- 无权限返回 403；
- 跨配送商目标返回 404/拒绝且数据库无变化；
- 请求体伪造 `disId/operatorUserId` 无法越权；
- 同键同请求只执行一次，同键不同请求返回 409；
- 两个用户基于同一版本确认时只有一个成功；
- 每个成功、拒绝、失败操作可按 `requestId/operationId` 追踪；
- Boss 现有主链在测试环境回归通过；
- Electron 仍没有可见写按钮。

### Slice 2.1：Electron 命令基础设施，功能开关关闭

**范围：**

- 登录权限和服务端 capabilities 读取；
- 固定 actionType 白名单；
- `dispatchCommandApi`、幂等键、统一错误处理；
- 确认弹窗和动作后读模型刷新；
- 按配送商、用户、版本控制的服务端功能开关。

**验收标准：**

- 客户端不能执行服务端下发的任意 URL；
- 权限缺失时按钮不显示，服务端仍能独立拒绝伪造请求；
- 超时重试复用同一个幂等键；
- 命令层不调用 Electron 系统命令、MCP 或打印 IPC；
- 功能开关关闭时行为与 Slice 1 完全一致。

### Slice 2.2：单站分配司机

**范围：**

- 打开服务端司机全景；
- 选择司机；
- 服务端预览；
- 风险二次确认；
- 幂等确认分配；
- 成功后刷新三阶段和司机状态。

**验收标准：**

- OFF_DUTY、跨配送商或服务端禁用司机不能被分配；
- 订单状态变化后确认被服务端拒绝；
- 双击、断网重试只产生一次分配；
- 两名调度员同时分配同一站点只有一个有效结果；
- Electron 不计算“司机可用”。

### Slice 2.3：路线调整与站点排序

**范围：**

- 单司机路线草稿；
- 拖拽/上移/下移；
- 服务端 preview；
- 风险和时间窗展示；
- 带 preview token 和版本的原子确认。

**验收标准：**

- 拖拽不直接写库；
- 未 preview 不能确认；
- 服务端禁用时 Electron 不得覆盖；
- 版本冲突强制刷新和重新试算；
- 已配送站点不能被移除或重排；
- 审计能还原调整前后顺序。

### Slice 2.4：确认派单并进入装车

**范围：**

- 确认派单弹窗；
- 服务端最终校验；
- 当前兼容语义下确认后进入 `LOADING`；
- 返回并定位装车阶段。

**验收标准：**

- 状态推进完全取服务端结果；
- 重复提交不重复写确认时间；
- 确认失败不在本地切换阶段；
- 操作完成后分派中、装车中和司机列表一致；
- 打印、Bills、OCR、MCP 回归通过。

### Slice 2.5：装车确认决策点

**默认方案：**保持只读装车状态，不增加写按钮。

只有业务方确认需要显式“整车装车完成”或“逐站装车确认”，并且服务端完成对应状态、
接口和发车策略后，才实施写操作。

**验收标准：**

- 打印成功和物理装车确认不会被错误合并；
- 服务端关闭强制装车时保持当前发车兼容行为；
- 服务端开启强制装车时，未满足条件不能发车；
- 重复装车确认不改写首次确认时间。

### Slice 2.6：发车确认

**范围：**

- 路线级发车；
- 服务端发车策略统一校验；
- 未打印单据等 warning 展示；
- 状态冲突处理。

**验收标准：**

- 发车命令不会临时创建空路线；
- 未确认、司机未当班、不可行、已发车路线均被正确处理；
- `actualDepartAt` 只写一次；
- 发车后路线和任务统一进入服务端规定状态；
- 发车后受限制的路线编辑/退回命令由服务端拒绝。

### Slice 2.7：异常登记与处置

**范围：**

- 异常类型字典；
- 异常登记；
- 服务端允许的处置动作；
- 原因、备注和可选凭证；
- 异常时间线与审计。

**验收标准：**

- 异常只可登记到有权限且处于允许状态的任务；
- 司机只能操作自己的当前任务；
- 重复登记和重复处置受幂等保护；
- 状态推进与路线释放完全由服务端完成；
- 原始异常记录不可覆盖；
- “退回沙盘”和“确认送达”只作为服务端允许的处置结果。

### Slice 2.8：灰度与生产验收

**范围：**

- 管理员白名单灰度；
- 指标、告警和审计查询；
- 权限和功能开关回滚；
- 多电脑并发、断网重试和旧 Boss 端兼容测试。

**验收标准：**

- 可按配送商、用户和 Electron 版本关闭写能力；
- 服务异常时自动退回只读，不影响打印；
- 可监控成功率、409 冲突、401/403、规则拒绝和接口耗时；
- 可通过 `operationId` 定位一次操作的请求、审计和业务结果；
- 关闭 Slice 2 后 Slice 1 只读工作台立即可用；
- 打印、OCR、Bills、Excel、MCP 打印全量回归通过。

## 12. 上线门禁

以下任一条件不满足，对应写操作不得进入生产：

- 写接口仍处于匿名访问；
- 服务端仍信任客户端 `disId/operatorUserId`；
- 没有配送商数据归属校验；
- 没有服务端权限；
- 没有持久化幂等；
- 路线确认没有乐观锁；
- 没有成功和拒绝审计；
- 领域服务没有重新校验状态；
- Electron 可以执行任意服务端 URL；
- 写操作失败会在本地先改变状态；
- 打印、Bills、OCR 或 MCP 回归失败。

装车确认和通用异常处理还分别增加两个专属门禁：

- 没有服务端明确装车状态合同，不开放装车确认；
- 没有异常登记、允许处置集合和状态机，不开放异常处理。

## 13. 本次设计依据

本方案按当前代码而不是历史接口说明进行核对，关键依据包括：

- `nongxinle-server/src/main/resources/spring-shiro.xml`：
  当前 `/api/**=anon`；
- `nongxinle-server/src/main/java/com/nongxinle/dispatch/controller/TodayDispatchController.java`：
  当前调度写入口和缺少方法级权限；
- `boss-mini/subPackage-routeDispatch/pages/routeDispatch/_session.js`：
  当前从本地缓存取 `disId/operatorUserId`；
- `nongxinle-server/src/main/java/com/nongxinle/entity/NxDistributerUserEntity.java`、
  `NxDistributerTypeUtils.java`：当前配送商用户岗位类型；
- `TodayDispatchRouteConfirmService`、`TodayDispatchRouteEditService`：
  路线确认事务、站点更新和确认后进入装车语义；
- `DisRouteDriverDepartPolicy` 与
  `DisRouteSandboxDriverDepartNowServiceImpl`：已有发车规则和当前写服务之间的校验缺口；
- `DisShipmentTaskService.onBillPrinted`：打印完成推进 `READY_TO_GO` 的现有语义；
- `boss-mini/.../myDelivery.js`：通用 `MARK_DELIVERY_EXCEPTION` 尚未接入；
- `NxDisDriverRouteEntity`、`NxDisShipmentTaskEntity` 及 MyBatis Mapper：
  当前没有聚合版本字段；
- `nx_dst_open_key` 唯一键：能避免部分活动任务重复，但不构成完整命令幂等。

## 14. 最终建议

Slice 2 不应从“做可点击按钮”开始，而应从 `nongxinle-server` 的写安全底座开始。

最先适合上线的业务写能力是“单站分配司机”，其次是“路线试算、调整和确认派单”。
发车要等现有发车策略真正接入写服务；装车确认要等业务语义和后端状态合同明确；
异常处理要等服务端补齐登记与处置闭环。

全程保持：

```text
Electron = 展示 + 采集意图 + 提交命令 + 展示结果
nongxinle-server = 身份 + 权限 + 规则 + 计算 + 状态机 + 幂等 + 审计
```

这条边界也是桌面调度工作台能够安全承载真实派单业务的前提。
