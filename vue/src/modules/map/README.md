# map 模块边界

`map` 负责 `mapOverview` 的只读渲染适配。

Slice 1 使用服务端提供的仓库、marker、polyline、图例和缺失坐标信息绘制桌面路线概览，
不增加地图 Key，不访问额外地图接口。

它不负责：

- 路线规划；
- 客户排序；
- 距离和预计时长计算；
- 司机可派判断。

当前坐标视图不是新的路线引擎，也不生成导航结果。后续若接入既有地图 SDK，只允许
替换底图渲染层，业务坐标和线路仍以服务端 `mapOverview` 为准。
