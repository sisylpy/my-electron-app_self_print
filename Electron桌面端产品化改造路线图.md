# Electron 桌面端产品化改造路线图

> 产品：农心乐配送商系统  
> 项目：`ai-priter-electic`  
> 日期：2026-07-30  
> 阶段：派单模块完成后的桌面产品统一规划  
> 本文性质：实施路线图；当前不进行大规模重构

## 1. 结论

当前 Electron 已经同时具备：

- OCR 与 AI 录单；
- Excel、图片、粘贴和手工录入；
- 订单处理；
- 六种现场打印模板；
- Epson 等真实设备的人工校准能力；
- MCP 配送单打印；
- 桌面调度工作台；
- 扫码登录、分配司机和确认派单。

这些能力已经能够覆盖配送商从“收到订单”到“派单、打印”的主要桌面工作，但产品形态仍处于“多个功能先后接入同一个安装包”的阶段：

- 首页、打印页和调度页各自维护布局；
- `Bills.vue` 同时承载订单、录入、客户和打印入口；
- OCR、AI、Excel 更像多个实现能力，而不是统一的录单流程；
- 调度工作台已经形成新的视觉标准，但没有公共设计系统；
- 主进程和录单组件仍然过大；
- 打印逻辑经过现场适配，不能跟随普通 UI 重构一起调整；
- 自动更新、签名、崩溃恢复和统一配置仍不完整。

因此本轮不重写业务，而采用以下路径：

```text
先统一产品结构
    ↓
再建立公共视觉语言
    ↓
再拆非打印超大组件
    ↓
冻结并专项治理打印系统
    ↓
最后完成发布、更新、日志和异常恢复
```

最终目标不是把现有能力做成六个顶级工具，而是形成一个完整的配送商桌面工作流：

```text
订单进入
  ├─ OCR / AI
  ├─ Excel
  ├─ 图片 / 粘贴
  └─ 手工录入
        ↓
订单校验与保存
        ↓
桌面调度
        ↓
配送单打印 / 装车准备
```

## 2. 改造原则

### 2.1 必须保持

1. 不修改 `nongxinle-server` 的派单规则、路线计算和司机状态判断；
2. 不新增派单业务范围；
3. 不改变 OCR、AI、Excel 的业务输入和服务端合同；
4. 不改变现有六种打印模板的分页、尺寸、边距和走纸语义；
5. MCP 继续复用 Bills、Vue 打印模板和 Electron 正式打印链路；
6. 现有 `/bills`、`/print`、`/dispatch` 等入口继续兼容；
7. 每个阶段都允许独立回滚，不做一次性整体替换。

### 2.2 明确禁止

- 同时做 UI 改版和业务逻辑重写；
- 在大文件拆分时顺手修改接口、状态机或缓存语义；
- 在没有 Epson 实物基线前抽象、合并六种打印模板；
- 把调度规则迁入 Electron；
- 为追求目录整齐而机械移动大量代码；
- 在同一个切片中同时升级 Electron、重构主进程和修改打印；
- 直接删除历史打印参数、等待时间或 240/241 mm 差异。

## 3. 当前基线

### 3.1 路由

当前正式路由：

| 路径 | 页面 | 当前定位 |
|---|---|---|
| `/` | `Screen.vue` | 广告/打印机入口页 |
| `/home` | `Home.vue` | 配送商登录和进入 Bills |
| `/bills` | `Bills.vue` | 订单、录单、客户、打印综合页 |
| `/print` | `/bills` 别名 | 打印兼容入口 |
| `/dispatch` | `DispatchWorkbench.vue` | 桌面调度工作台 |

现有路由已经由一个 Vue Router 统一注册，打印和调度保持独立路由，这是正确基础。

当前问题是缺少统一的父级产品布局。每个页面自己决定：

- 顶部身份区；
- 页面标题；
- 菜单入口；
- 返回方式；
- 状态提示；
- 内容宽度和间距。

### 3.2 Vue 模块

已经建立：

```text
vue/src/modules/
├── print
├── dispatch
├── order
└── map
```

当前成熟度不同：

- `dispatch` 已包含路由、API、服务、状态、组件和页面；
- `print` 目前主要提供路由边界，实际代码仍在 `Bills.vue`、`Applys/` 和主进程；
- `order` 目前只承担少量只读查询；
- `map` 是调度地图的只读展示适配。

下一阶段应保留这些边界，但不追求一次性把旧代码全部搬入模块目录。

### 3.3 状态管理

根 Vuex 当前保存：

- `disUser`；
- 全局 `loading`；
- `mcpPrintQueue`；
- 少量历史兼容状态。

`dispatch` 已经采用 namespaced Vuex，并坚持：

- 服务端快照只保存在内存；
- 不在前端推进调度状态；
- 不持久化派单业务结果；
- 写操作通过 preload 白名单进入主进程。

主要问题：

- `disUser` 完整对象仍是多个功能的隐式依赖；
- `Bills.vue` 和大型录单组件拥有大量本地状态；
- MCP 队列仍属于根状态；
- “页面状态、业务草稿、服务端快照、设备状态”尚未形成统一分类；
- 多处直接使用 `localStorage`，缺少统一缓存目录和生命周期。

### 3.4 主布局和菜单

当前 `App.vue` 主要只负责全局 Loading、MCP 打印事件和 `router-view`，不是产品壳层。

Electron 原生菜单主要包含：

- 文件、编辑、视图、窗口、帮助；
- 取消自动登录；
- 打开日志目录；
- 关于。

原生菜单还没有体现产品业务结构；页面内也没有稳定的全局菜单。当前 Bills 新增的“派单工作台”按钮解决了可达性，但还不是最终统一导航。

### 3.5 超大文件

当前主要大文件：

| 文件 | 行数 | 处理原则 |
|---|---:|---|
| `PlaceOrder.vue` | 8511 | P2 第一优先，先拆录入模式、服务和草稿状态 |
| `TaskOrder.vue` | 4968 | P2 第二优先，拆任务列表、编辑区和任务轮询 |
| `src/main.js` | 3579 | P4 按能力拆主进程，不在 P2 与 Vue 同时修改 |
| `OrderList.vue` | 3474 | P2 第三优先，拆查询、列表和操作区 |
| `ApplyFiftyPanel.vue` | 3439 | 打印保护区，延后到 P3 实物基线以后 |
| `ApplyHalfPanel.vue` | 2954 | 打印保护区 |
| `ApplyThirtyWholePanel.vue` | 2938 | 打印保护区 |
| `Bills.vue` | 2596 | P0/P1 只增加壳层适配；P2 再拆页面协调逻辑 |
| `TodayOrders.vue` | 2649 | 随订单模块逐步拆分 |
| `DispatchWorkbench.vue` | 941 | 已有合理组件边界，可作为新页面标准 |

### 3.6 产品发布能力

已具备：

- 主窗口 `nodeIntegration: false`；
- 主窗口 `contextIsolation: true`；
- 主窗口启用 `sandbox`；
- 非可信导航和新窗口已阻止；
- preload 已改为具名白名单；
- 调度 Token 使用主进程和系统安全存储；
- MCP 已增加回环地址、Token 和来源限制；
- 有基础应用日志和未捕获异常记录；
- 已有 macOS/Windows 打包脚本。

仍需完成：

- Electron 仍为 19.x，升级尚未实施；
- `electron-builder` 仍为 23.x；
- `asar: false`；
- `publish: null`，没有自动更新链路；
- 没有正式签名、公证和发布通道配置；
- 没有单实例锁；
- 没有统一处理 renderer 崩溃、无响应和恢复；
- 日志没有完整的轮转、容量上限和字段脱敏策略；
- 开发、测试、生产 API 配置仍分散；
- 产品名称仍混用 `GrainPrint`、`订单管理系统`、`My Electron App` 等历史名称；
- 缺少统一版本展示和诊断信息页。

## 4. 目标产品结构

### 4.1 信息架构

建议的一级导航：

| 一级入口 | 内容 | 说明 |
|---|---|---|
| 今日工作台 | 今日订单、待处理任务、派单状态、打印状态 | 只做汇总和快捷入口，不复制业务状态 |
| 订单与录入 | OCR、AI、Excel、图片、粘贴、手工录入、任务处理 | OCR/AI/Excel 是录入方式，不再是独立产品 |
| 配送调度 | 订单、司机、路线、地图、确认派单 | 继续使用现有 DispatchWorkbench |
| 打印中心 | 配送单、客户、打印队列、历史打印 | 继续复用 Bills 和现有打印模板 |
| 设备与设置 | 打印机、纸型校准、MCP、日志、版本、更新 | 高权限和诊断能力集中管理 |

MCP 不建议作为顶级业务模块。它是桌面集成能力，应在全局状态栏显示运行状态，在“设备与设置”中管理。

### 4.2 统一产品壳层

新增公共 `ProductShell`，只承担产品级职责：

```text
ProductShell
├── GlobalSidebar
│   ├── 今日工作台
│   ├── 订单与录入
│   ├── 配送调度
│   ├── 打印中心
│   └── 设备与设置
├── GlobalTopbar
│   ├── 当前配送商
│   ├── 当前账号
│   ├── 服务连接状态
│   ├── 默认打印机
│   ├── MCP 状态
│   └── 全局消息
├── PageHeader
│   ├── 页面标题
│   ├── 面包屑
│   ├── 状态摘要
│   └── 页面级操作
└── RouterView
```

壳层不得：

- 接管订单、派单或打印业务状态；
- 直接调用业务写接口；
- 读取打印模板内部数据；
- 自动切换派单状态；
- 在路由切换时清空 MCP 打印队列。

### 4.3 目标路由

建议采用嵌套路由，同时保留旧路径别名：

```text
/
├── /workbench
├── /orders
│   ├── /orders/intake
│   ├── /orders/tasks
│   └── /orders/customers
├── /dispatch
├── /print
│   ├── /print/delivery
│   ├── /print/history
│   └── /print/queue
└── /settings
    ├── /settings/printers
    ├── /settings/calibration
    ├── /settings/mcp
    ├── /settings/logs
    └── /settings/about
```

兼容要求：

- `/bills` 继续有效并进入打印中心；
- `/print` 继续有效；
- `/dispatch` 路径和路由名继续有效；
- MCP 仍强制导航到 Bills/打印流程，不导航到调度；
- 旧收藏、旧安装包跳转和记住用户逻辑继续可用。

### 4.4 目标模块结构

渐进式目标：

```text
vue/src/
├── app/
│   ├── layout/
│   ├── navigation/
│   └── runtime/
├── shared/
│   ├── ui/
│   ├── api/
│   ├── status/
│   └── utils/
└── modules/
    ├── session/
    ├── order/
    │   ├── intake/
    │   ├── tasks/
    │   ├── customers/
    │   ├── components/
    │   ├── services/
    │   └── store/
    ├── dispatch/
    ├── print/
    │   ├── legacy/
    │   ├── profiles/
    │   └── orchestration/
    ├── map/
    └── system/
```

`legacy/` 不是永久垃圾目录，而是明确标记“已经稳定、尚未完成契约化迁移”的现场实现。

### 4.5 状态边界

| 状态类别 | 推荐归属 | 持久化 |
|---|---|---|
| 当前配送商与用户摘要 | `session` store | 只保存必要摘要；可信身份由服务端决定 |
| 页面导航和壳层状态 | `app` store | 可保存非敏感偏好 |
| 录单草稿 | `order/intake` store | 可按业务需要恢复，必须有版本和租户键 |
| 服务端订单/任务快照 | 对应领域 store | 默认内存；按接口刷新 |
| 调度快照 | `dispatch` store | 仅内存 |
| 打印任务队列 | `print` store，先提供根状态兼容门面 | 必须可追踪，不把 HTML 长期落盘 |
| 打印机 Profile | 主进程配置服务 | `userData`，版本化、原子写入、可恢复 |
| 调度 Token | 主进程安全凭证服务 | `safeStorage` |
| MCP Token | 主进程 MCP 安全模块 | 不进入渲染进程日志 |

## 5. P0：产品结构统一

目标：先让整个应用看起来和运行起来像一个产品，不改变各业务页内部逻辑。

### P0.0 基线冻结

动作：

1. 固化当前路由、MCP 跳转、登录恢复和打印入口契约；
2. 记录 Bills、录单、调度页面的关键截图；
3. 保存现有打印契约测试结果；
4. 建立核心用户路径清单；
5. 建立旧路径兼容测试。

验收：

- `/bills`、`/print`、`/dispatch` 均可直接打开；
- MCP 任务仍到 Bills；
- 调度不会清空或消费打印队列；
- 当前打印测试和 Slice 0/1/3 测试全部通过。

### P0.1 建立 ProductShell

动作：

1. 新增壳层、全局侧栏、顶部状态栏和统一页面标题；
2. 首先把现有页面原样嵌入壳层；
3. 不修改 Bills、PlaceOrder、TaskOrder 和打印模板的内部 DOM；
4. 在顶栏展示当前配送商、后端连接、打印机和 MCP 摘要；
5. 统一提供“回到工作台”和跨模块导航。

风险：

- 全局 CSS 可能污染打印样式；
- 路由缓存和组件卸载可能影响 OCR 任务或 MCP 队列。

控制：

- 壳层样式使用命名空间；
- 打印模板和隐藏打印窗口不加载壳层；
- 在切换路由前后验证任务轮询和 MCP FIFO；
- 不在本切片引入 keep-alive 策略变化。

验收：

- 所有业务页面从同一菜单可达；
- 页面切换不重新登录；
- 切换到调度后返回打印，原打印流程仍可用；
- MCP 打印不受当前页面影响；
- 没有全局 CSS 进入 `.print-page` 和 `@page` 规则。

### P0.2 路由和菜单配置化

动作：

1. 建立单一菜单配置；
2. 菜单项引用路由名，不在组件中散落字符串路径；
3. 路由 meta 明确模块、标题、权限和是否需要配送商会话；
4. 保留旧入口别名和重定向；
5. Electron 原生菜单只保留系统能力和诊断入口，业务导航以页面菜单为主。

验收：

- 菜单和路由来源一致；
- 未登录、无配送商和无权限状态有统一处理；
- 深链接启动后仍能恢复正确页面；
- 旧路径自动进入等价新页面。

### P0.3 状态和 API 边界整理

动作：

1. 列出根 Vuex 每个字段的拥有者；
2. 新增 `session` 和 `app` 状态模块，但先提供兼容 getter/mutation；
3. 建立共享 API 错误模型和连接状态；
4. 禁止页面新增裸 Axios 调用；
5. 禁止把服务端业务状态复制到多个 store；
6. 明确 localStorage 白名单、租户键、版本和清理规则。

验收：

- `disUser` 迁移不要求所有组件一次性改完；
- 调度继续以服务端 pageViewModel 为唯一业务数据源；
- 打印队列不丢失、不重复；
- 退出登录能够清除属于当前租户的敏感缓存；
- API 401、403、409、422 和网络错误使用统一外观，但保留各业务语义。

## 6. P1：UI 统一

目标：以 DispatchWorkbench 为视觉基线，建立可复用设计系统，不直接重写业务页面。

### 6.1 设计令牌

先抽取：

- 主色、成功、警告、错误和中性色；
- 页面背景、卡片背景和边框；
- 4/8/12/16/24/32 间距体系；
- 字号、行高和标题层级；
- 圆角、阴影和交互状态；
- 桌面最小点击区域；
- 左、中、右三栏的标准宽度和收缩规则。

设计令牌只作用于应用 UI。打印 HTML 必须使用独立打印变量，不共享会改变物理输出的全局字号、行高和间距。

### 6.2 公共组件

建议优先建立：

```text
AppShell
PageHeader
StatusBadge
MetricCard
InfoCard
ActionBar
EmptyState
LoadingState
ErrorState
ConfirmDialog
ToastCenter
ConnectionIndicator
PrinterStatus
```

### 6.3 迁移顺序

1. 壳层、页面标题和全局状态；
2. Bills 左侧导航和非打印空状态；
3. 订单/录单的卡片、按钮、反馈和弹窗；
4. 客户与任务列表；
5. 打印设置和校准页面的普通 UI；
6. Screen/Home 的登录和启动体验；
7. 最后处理不影响物理输出的打印预览外围。

不修改：

- 打印模板内部表格；
- `@page`；
- 物理尺寸；
- 业务分页；
- 打印专用字体、行高和边距。

### 6.4 UI 验收

- 相同状态使用相同颜色和文案；
- 所有页面都有统一加载、空、失败和部分失败状态；
- 主要操作位置稳定；
- 1366×768、1920×1080 和当前现场分辨率可用；
- 键盘焦点和确认弹窗行为一致；
- 调度、录单和打印之间切换时没有明显视觉断裂；
- 打印实物输出与 UI 改造前一致。

## 7. P2：大文件拆分

目标：降低修改风险，不改变业务行为。

### 7.1 第一批：非打印文件

#### PlaceOrder.vue

建议拆分：

```text
order/intake/components/
├── IntakeModeTabs.vue
├── CustomerSelector.vue
├── ManualEntryPanel.vue
├── ImageIntakePanel.vue
├── ExcelIntakePanel.vue
├── PasteIntakePanel.vue
├── DraftOrderPanel.vue
└── IntakeResultPanel.vue

order/intake/services/
├── intake-api.js
├── ocr-service.js
├── ai-normalize-service.js
├── excel-import-service.js
├── draft-cache-service.js
└── order-payload-mapper.js

order/intake/store/
└── index.js
```

原则：

- 先抽纯函数和 API；
- 再抽无状态展示组件；
- 最后移动草稿和任务状态；
- 每一步保持原事件名、参数和返回值。

#### TaskOrder.vue

建议拆分：

```text
order/tasks/components/
├── TaskCustomerList.vue
├── TaskToolbar.vue
├── TaskEditor.vue
├── TaskProgress.vue
└── TaskResultPanel.vue

order/tasks/services/
├── task-api.js
├── task-polling-service.js
└── task-payload-mapper.js

order/tasks/store/
└── index.js
```

重点保护：

- 当前轮询频率；
- 任务恢复；
- 切换客户时的清理语义；
- OCR/Excel 任务结果回填；
- Bills 传入和组件事件合同。

#### OrderList.vue

拆出：

- 查询和筛选服务；
- 订单行和批量操作组件；
- 分页/加载状态；
- 订单选择状态；
- 与打印入口的适配层。

### 7.2 第二批：页面协调层

`Bills.vue` 最终只保留：

- 当前子页面选择；
- 客户/任务/打印区域组合；
- 路由和模块间协调；
- MCP 打印兼容入口。

API、客户查询、模板解析、轮询和缓存应逐步移入服务或领域 store。

### 7.3 打印文件例外

`ApplyFiftyPanel.vue` 虽然超过 3000 行，但不能在普通 P2 中拆分。

六个 `Apply*Panel` 必须等 P3 完成：

- Epson 黄金样本；
- HTML/CSS 快照；
- 分页契约；
- IPC 契约；
- 多页打印与保存记录回归；

之后才能一次迁移一个模板。

### 7.4 拆分门禁

每个拆分提交必须满足：

1. 不修改后端接口；
2. 不改事件、props 和业务状态枚举；
3. 不同时改 UI；
4. 不同时引入 TypeScript 全量迁移；
5. 构建通过；
6. 关键路径回归通过；
7. 旧组件可快速回退；
8. 差异主要是移动和引用关系，而不是算法重写。

阶段目标：

- 非打印 Vue 文件不再超过 3000 行；
- 页面容器不再同时承担 API、缓存、轮询、状态机和全部 UI；
- 新增业务逻辑必须进入对应 service/store；
- 打印模板行数不作为本阶段强制指标。

## 8. P3：打印系统专项检查

目标：把现场经验变成可追踪、可回归、可配置的产品能力，同时保持现有打印结果。

### 8.1 当前受保护基线

| 纸型 | 模板 | 尺寸 | 分页 | 逐页等待 |
|---|---|---:|---:|---:|
| 全张一列 | `ApplyPanel` | 240 × 279 mm | 30 | 1500 ms |
| 全张两列 | `ApplyFiftyPanel` | 240 × 279 mm | 60 | 4000 ms |
| 1/2 一列 | `ApplyHalfWholePanel` | 240 × 140 mm | 12 | 400 ms |
| 1/2 两列 | `ApplyHalfPanel` | 240 × 140 mm | 26 | 400 ms |
| 1/3 一列 | `ApplyThirtyWholePanel` | 240/241 × 93 mm | 5 | 400 ms |
| 1/3 两列 | `ApplyThirtyPanel` | 240 × 93 mm | 12 | 400 ms |

必须继续保护：

- Epson 120×60 DPI 的现场处理经验；
- 正式打印不强制 DPI、优先由驱动决定的现状；
- `marginsType` 和模板安全边距的现有组合；
- 隐藏打印窗口 `zoomFactor = 1.0`；
- 210 mm 安全基准宽度；
- 240/241 mm 历史差异；
- 逐页打印；
- 只在最后一页保存打印记录；
- 普通、国标、批次和历史打印的不同保存语义；
- MCP 复用同一打印链。

### 8.2 P3.0 黄金样本

建立实物矩阵：

```text
设备品牌/型号
× 操作系统
× 驱动版本
× 全张/1-2/1-3
× 单列/双列
× 临界行数/多一行/多页/长商品名
× 普通打印/MCP 打印
```

每个样本保存：

- 输入订单；
- 生成 HTML；
- 打印参数；
- Profile；
- 驱动配置；
- 实物照片；
- 页数和分页点；
- 是否裁切、偏移、重叠；
- 最后一次业务保存结果。

### 8.3 P3.1 非设备自动门禁

增加：

- 六模板分页契约测试；
- 纸型、尺寸、行数和等待时间常量快照；
- 正式打印 HTML/CSS 快照；
- preload 打印方法契约；
- 主进程打印参数契约；
- 多页只保存一次记录测试；
- MCP → Bills → 模板 → IPC 路径测试。

### 8.4 P3.2 Profile 产品化

在不改变旧算法的前提下增加：

- `schemaVersion`；
- 电脑、设备、驱动和纸型标识；
- 原子写入；
- 上一版本备份；
- 损坏恢复；
- 导入/导出；
- 修改人和更新时间；
- “已验证/未验证”状态；
- 一键回到 `legacy-v1`。

配置优先级：

```text
临时覆盖
> 电脑 + 打印机 + 驱动 + 纸型 + 模板
> 打印机 + 纸型
> 品牌/型号推荐
> 旧打印机名称 Profile
> legacy-v1 默认值
```

### 8.5 P3.3 多品牌演进

不使用散落的打印机名称判断，而建立设备适配描述：

- 能力探测；
- 驱动 PageSize 名称；
- 推荐 DPI；
- 不可打印区域；
- 默认原点偏移；
- 推荐走纸等待；
- 已验证驱动版本；
- 已知限制。

品牌默认值只能作为校准起点，最终仍以当前电脑、驱动和设备实测为准。

### 8.6 打印 Go/No-Go

任何涉及打印模板、主进程打印选项或 Profile 计算的发布必须满足：

- Epson 当前现场结果不低于黄金样本；
- 六模板分页一致；
- 多页没有重复或漏保存记录；
- MCP 和手工打印输出一致；
- 旧 Profile 可读取；
- 新 Profile 可回退；
- 其他品牌至少完成一套明确设备矩阵；
- 未通过实物验收时默认继续使用 `legacy-v1`。

## 9. P4：产品级检查

### 9.1 Electron 安全

继续保留当前成果：

- 主窗口沙箱；
- context isolation；
- preload 白名单；
- 来源 URL 校验；
- 禁止新窗口和非可信导航；
- safeStorage 调度凭证；
- MCP 鉴权。

下一步：

1. 所有打印窗口也明确设置安全参数；
2. 为 IPC 参数引入共享 schema；
3. 文件访问从路径传递继续演进为用户授权目录能力；
4. 增加 CSP；
5. 生产版禁用开发者工具和危险快捷入口；
6. 安装包、源码和产物执行密钥扫描；
7. 为 MCP 增加启停策略、限流和审计保留；
8. 建立权限清单和 IPC 清单。

### 9.2 Electron 升级

采用独立切片：

1. 冻结 Electron 19 的启动、OCR、文件、打印和 MCP 基线；
2. 只升级 Electron/Builder，不改业务代码；
3. 检查原生依赖；
4. Windows/macOS 分别验证；
5. 六模板完成实物回归；
6. 任一打印差异无法解释时停止升级并回退。

不得与主进程拆分或打印模板重构同批进行。

### 9.3 主进程拆分

目标：

```text
src/main/
├── bootstrap/
├── windows/
├── ipc/
├── services/
│   ├── printing/
│   ├── files/
│   ├── voice/
│   ├── updates/
│   ├── mcp/
│   └── dispatch/
├── infrastructure/
│   ├── config/
│   ├── http/
│   ├── logging/
│   └── storage/
└── security/
```

拆分顺序：

1. 纯配置和参数校验；
2. 日志；
3. 已独立的 dispatch gateway；
4. MCP 生命周期；
5. 文件能力；
6. 语音能力；
7. 打印机配置；
8. 最后拆正式打印编排。

### 9.4 自动更新与签名

当前 `publish: null`，不具备产品自动更新链。

需要建立：

- 测试、灰度、正式三个发布通道；
- macOS Developer ID、Hardened Runtime、公证；
- Windows 代码签名；
- 签名后的更新清单；
- 下载完整性验证；
- 强制/可选更新策略；
- 更新失败恢复；
- 安装和更新期间保留 `userData`；
- 打印配置迁移和回滚；
- 版本降级策略；
- 更新日志。

禁止无签名自动更新。

### 9.5 日志和诊断

当前已有按天日志和异常捕获，下一步增加：

- 日志轮转和容量上限；
- Token、手机号、地址、订单明细脱敏；
- `requestId`、`operationId`、MCP taskId 和打印 jobId 关联；
- 用户可导出的诊断包；
- 诊断包内容白名单；
- 当前版本、系统、驱动、打印机、后端环境和关键配置摘要；
- 不导出 Token、完整订单或客户隐私。

### 9.6 崩溃和异常恢复

增加：

- 单实例锁；
- renderer `render-process-gone`；
- `unresponsive`；
- 启动失败和白屏恢复页；
- 安全重载；
- 未完成录单草稿恢复；
- MCP 队列 ACK 和超时状态；
- 打印任务“已提交/失败/状态未知”区分；
- 连续崩溃后的安全模式；
- 恢复时不自动重复提交派单或重复打印。

### 9.7 配置管理

统一配置层至少区分：

```text
构建期公开配置
运行环境配置
设备本地配置
用户偏好
安全凭证
业务服务端配置
```

目标：

- API 地址不再散落；
- 开发、测试、生产环境明确显示；
- 安全凭证不进入普通 JSON；
- 配置有 schema 和版本；
- 非法配置拒绝启动高风险能力；
- 页面能够查看当前环境，但不能看到密钥。

## 10. 推荐实施切片

| 顺序 | 切片 | 主要内容 | 是否触碰打印核心 |
|---:|---|---|---|
| 1 | P0.0 | 产品、路由、打印和关键截图基线 | 否 |
| 2 | P0.1 | ProductShell 和统一导航 | 否 |
| 3 | P0.2 | 路由 meta、旧路径兼容、菜单配置 | 否 |
| 4 | P0.3 | session/app 状态边界、统一 API 错误 | 否 |
| 5 | P1.0 | 设计令牌和公共状态组件 | 否 |
| 6 | P1.1 | Bills、录单、任务外围 UI 统一 | 不修改打印 DOM |
| 7 | P2.0 | PlaceOrder 服务和无状态组件拆分 | 否 |
| 8 | P2.1 | TaskOrder 拆分 | 否 |
| 9 | P2.2 | OrderList、Bills 协调层拆分 | 否 |
| 10 | P3.0 | Epson 黄金样本和打印自动门禁 | 只观测 |
| 11 | P3.1 | Profile 版本化、备份和恢复 | 保留 legacy |
| 12 | P3.2 | 多品牌设备矩阵和校准流程 | 可回滚 |
| 13 | P4.0 | 主进程非打印能力拆分 | 否 |
| 14 | P4.1 | Electron 升级与跨平台回归 | 只验证打印 |
| 15 | P4.2 | 签名、更新、日志和异常恢复 | 不改打印算法 |
| 16 | RC | 全产品回归和灰度发布 | 全量验收 |

## 11. 主要风险

| 风险 | 影响 | 控制措施 |
|---|---|---|
| 全局 UI 样式污染打印 | 纸张偏移、分页变化 | 壳层样式命名空间；打印 DOM 独立；实物门禁 |
| 路由重组影响 MCP | 打印任务丢失或重复 | 保留 Bills 路由名和兼容入口；FIFO/ACK 测试 |
| 大文件拆分改变异步时序 | OCR、AI、任务轮询回归 | 先抽纯服务；保持事件合同；逐文件拆分 |
| store 迁移导致缓存串租户 | 错误客户或草稿 | 所有缓存加入 disId/userId/schemaVersion |
| Electron 升级影响打印 | DPI、边距或窗口加载变化 | 独立升级切片；六模板实物回归；可回退安装包 |
| 自动更新破坏配置 | 现场校准丢失 | userData 迁移测试、备份和版本回退 |
| 日志记录隐私 | 客户、订单、Token 泄露 | 字段白名单和统一脱敏 |
| 多品牌抽象过早 | Epson 稳定结果被破坏 | legacy-v1 默认；每次只迁移一台设备/一个模板 |

## 12. 产品化完成定义

满足以下条件才视为本阶段完成：

1. 所有功能位于统一产品壳层，可从一个菜单进入；
2. OCR、AI、Excel 被表达为“订单录入方式”，不是割裂工具；
3. 订单、调度、打印之间可以清晰流转；
4. 页面使用统一卡片、间距、颜色、状态和空状态；
5. 非打印 Vue 组件不再超过 3000 行；
6. 业务 API、轮询、缓存和展示职责已分离；
7. 六种打印模板有自动契约和 Epson 实物黄金样本；
8. MCP 与手工打印继续使用同一正式链路；
9. Electron 安全门禁通过；
10. Windows/macOS 安装包具备正式签名；
11. 自动更新有灰度、失败恢复和回滚；
12. 日志可诊断但不泄露敏感数据；
13. 崩溃恢复不会重复派单或重复打印；
14. 测试、灰度和生产环境配置清晰可识别；
15. 所有旧入口和现场配置完成迁移或兼容。

## 13. 建议的下一步

下一步只进入 **P0.0 产品基线冻结与 P0.1 壳层详细设计**：

1. 绘制统一导航线框；
2. 冻结现有页面关键截图；
3. 建立旧路由、MCP 和打印回归清单；
4. 明确 ProductShell 不得接触的业务状态；
5. 确认菜单名称和页面层级；
6. 再开始小范围实现壳层。

在这一步确认之前，不拆 PlaceOrder，不改打印模板，不升级 Electron。
