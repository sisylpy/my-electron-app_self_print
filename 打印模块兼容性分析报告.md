# 打印模块兼容性分析报告

> 项目：农心乐配送商系统  
> 审查对象：`ai-priter-electic` 打印模块  
> 审查日期：2026-07-30  
> 报告性质：Electron 生产化整改前的打印保护基线  
> 当前动作：冻结打印行为；Phase 1 只收紧外围安全边界，不改页面、分页、边距、DPI 和走纸语义

## 1. 结论摘要

当前打印模块不是普通的网页打印，而是一套经过 Epson 等真实针式打印机现场调试后形成的“设备适配型打印引擎”。

实物结果由以下五层共同决定：

1. 客户配置选择的六种打印模板；
2. 模板内部的纸张尺寸、分页行数、字体、边距和布局计算；
3. 按打印机名称保存的人工校准参数；
4. Electron/Chromium 交给操作系统打印驱动的默认边距、DPI 和缩放；
5. 针式打印机实际走纸速度，以及代码中的逐页等待时间。

因此，本轮生产化整改必须遵守以下结论：

- 不直接删除人工参数、Epson 特殊注释、240/241 mm 差异、DPI 判断和逐页等待；
- 不把 1/3 纸、1/2 纸、全张纸简单改成三个 CSS 页面尺寸；
- 不在没有实物回归的情况下统一 `@page`、边距、缩放或打印选项；
- 先把现状固化为 `legacy-v1` 兼容模式，再建立标准纸型、设备画像和分层校准；
- Electron 安全整改可以收窄 IPC 和 MCP 边界，但不得改变打印 HTML、打印参数顺序、分页规则和打印任务语义。

静态代码审查能够确认流程、参数和风险，但无法证明某台打印机的实物输出正确。最终兼容结论必须通过真实 Epson 和至少一种其他品牌设备的物理打印测试确认。

## 2. 当前打印流程

```text
客户的 nxDepartmentPrintName
        ↓
Bills.vue / TodayOrders.vue 选择六种模板之一
        ↓
模板按业务数据分页，并生成单页完整 HTML + CSS
        ↓
读取当前默认打印机和该打印机的人工校准 Profile
        ↓
模板计算纸张尺寸、边距、内容宽高、字体和缩放
        ↓
preload 白名单打印方法
        ↓
主进程创建隐藏 BrowserWindow，zoomFactor 固定为 1.0
        ↓
webContents.print(silent + printBackground + deviceName)
        ↓
操作系统驱动执行 DPI、默认边距、驱动缩放和实际走纸
        ↓
Electron 回调成功后，最后一页保存打印记录
```

### 2.1 模板选择

当前 `Bills.vue` 和 `TodayOrders.vue` 支持以下六种模板。客户的 `nxDepartmentPrintName` 决定实际模板；不支持的名称回退到 `ApplyPanel`。

| 业务纸型 | 排版 | 模板 | 代码尺寸 | 每页业务行数 | 已发现的逐页等待 |
|---|---|---|---:|---:|---:|
| 全张纸 | 一列 | `ApplyPanel` | 240 × 279 mm | 30 | 1500 ms |
| 全张纸 | 两列 | `ApplyFiftyPanel` | 240 × 279 mm | 60（30 + 30） | 4000 ms |
| 1/2 纸 | 一列 | `ApplyHalfWholePanel` | 240 × 140 mm | 12 | 400 ms |
| 1/2 纸 | 两列 | `ApplyHalfPanel` | 240 × 140 mm | 26（13 + 13） | 400 ms |
| 1/3 纸 | 一列 | `ApplyThirtyWholePanel` | 240/241 × 93 mm | 5 | 400 ms |
| 1/3 纸 | 两列 | `ApplyThirtyPanel` | 240 × 93 mm | 12（6 + 6） | 400 ms |

注意：

- 表中的等待时间不是网络等待，而是对物理打印和走纸速度的现场补偿；
- 全张两列明确使用了 4000 ms，全张一列使用 1500 ms，说明不同排版的渲染量和实物走纸行为已经被分别调试；
- `ApplyThirtyWholePanel` 的部分正式打印路径使用 241 mm，部分预览或校准路径使用 240 mm。这是需要纳入实物基线的历史差异，不能直接认定为普通代码错误；
- `ApplyFiftyPanel` 的部分日志或辅助对象中也残留 241 mm，而正式 `@page` 和主要计算使用 240 mm。

### 2.2 单页打印与业务回执

六种模板不是把多页 HTML 一次性交给驱动，而是按业务分页后逐页生成 HTML，再逐页调用带回执的打印接口。

关键语义如下：

- `paperCount` 表示本次打印的总页数；
- `shouldSave` 只在最后一页为 `true`；
- 普通订单和国标订单使用不同的保存接口；
- 历史订单打印可以跳过保存打印记录；
- Electron 的 `webContents.print` 回调只表示任务被系统打印链路接受或拒绝，不代表纸张已经完成物理走纸；
- 模板中的 400/1500/4000 ms 等待，用来减少连续任务相互覆盖或走纸未完成的问题。

这些语义同时被普通界面打印和 MCP 打印流程依赖，应作为 Phase 1 回归门禁。

## 3. 页面尺寸与布局计算

### 3.1 当前尺寸不是单一来源

当前页面尺寸至少同时存在四种概念：

1. 业务纸型：全张、1/2、1/3；
2. `@page size`：例如 240 × 279 mm、240 × 140 mm、240/241 × 93 mm；
3. 代码中的 `physicalPageWidth/physicalPageHeight`；
4. 用于内容安全区计算的 210 mm 基准宽度。

主流模板的宽度计算可以概括为：

```text
原始可用宽度 = 210 - 左安全边距 - 右安全边距
缩放后可用宽度 = 原始可用宽度 × zoomFactor
缩放后左/右边距 = 原边距 × zoomFactor
容器总宽度 = 缩放后左边距 + 缩放后可用宽度 + 缩放后右边距
最终宽度再受 physicalPageWidth 和 maxPrintableWidth 限制
```

高度计算在多个模板中近似为：

```text
页面内容高度 = (物理纸高 - 10 mm 上部预留 - 5 mm 下部预留) × zoomFactor
```

字体、行高、标题字号也会再次乘以 `zoomFactor`。

### 3.2 当前边距实际由多层共同产生

现有系统没有一个唯一的“最终边距”：

- 模板 `@page` 多数设置 `margin: 0`，部分校准/预览样式使用 3 mm；
- 页面容器使用人工配置的左右安全边距或 padding；
- 普通回执打印在主进程使用 `marginsType: 0`，即系统默认边距；
- 校准页使用 `marginsType: 1`，即无边距；
- 部分旧普通/国标打印函数同时保留 `marginsType: 0` 和 `margins.marginType = none` 的历史组合；
- 打印机驱动还可能自行增加不可打印区域、居中、适配页面或缩放。

因此，用户看到的物理边距是“模板边距 + 应用安全边距 + Chromium 解释 + 驱动不可打印区/缩放”的合成结果。

### 3.3 当前缩放存在耦合

`zoomFactor` 当前同时影响：

- 左右安全边距；
- 内容区域宽度；
- 页面内容高度；
- 配送商标题字号；
- 表头字号；
- 订单字号；
- 部分行高和内部布局。

如果驱动本身还启用了“适合页面”或比例缩放，就可能形成应用缩放与驱动缩放的双重补偿。

这不是要求立即改写，而是说明未来应把缩放拆分为：

- 横向几何校准 `scaleX`；
- 纵向走纸校准 `scaleY/feedScale`；
- 内容字号和行高；
- 物理原点偏移 `offsetLeft/offsetTop`；
- 驱动策略。

在实物回归完成前，现有 `zoomFactor` 必须按原语义保留。

## 4. DPI 与驱动处理

### 4.1 正式打印当前不强制 DPI

主进程的 `getPrintDpiOptions()` 当前返回空对象。代码注释明确记录：

- Epson LQ-730K 等针式打印机可能向系统报告 120 × 60 DPI；
- 曾考虑显式传入 DPI；
- 当前选择不向 Electron 打印 API 强制传 DPI，由操作系统驱动使用系统默认值。

这说明“不设置 DPI”本身就是一次现场兼容决策，不应被当作遗漏直接补成 300 × 300。

### 4.2 系统配置读取

校准面板可以读取系统打印机信息：

- Windows：通过 WMI/PowerShell 获取打印机、水平/垂直 DPI 和纸张尺寸；
- macOS：通过 `lpstat` 获取默认打印机，通过 `lpoptions -l` 解析 Resolution 和 PageSize；
- 解析失败时会使用 300 × 300 DPI、241 × 279 mm 等兜底值。

这些系统值目前主要用于展示和人工判断，并没有形成稳定的自动布局输入。

### 4.3 macOS 分辨率写入

macOS 校准面板允许通过 `lpoptions` 把 120 × 60、180 × 180、360 × 180 等分辨率设为打印机系统默认值。

Phase 1 已在不改变参数含义的前提下，把该命令改为参数数组调用，并为打印机队列名和三种现有分辨率增加白名单。仍需注意：

- 修改的是操作系统打印机默认配置，不只影响本应用；
- 不同驱动暴露的 Resolution 枚举名称可能不同；
- 用户更换驱动后，同一型号可能返回不同能力集合；
- 从 shell 调用改为参数数组后，必须用真实设备确认命令返回和驱动结果一致。

## 5. 打印机配置保存方式

### 5.1 当前落盘位置

配置位于 Electron `app.getPath('userData')`：

| 文件 | 用途 |
|---|---|
| `printer-config.json` | 保存当前默认打印机名称和更新时间 |
| `printer-profiles.json` | 按打印机名称保存人工校准参数 |

`printer-profiles.json` 的键是系统返回的打印机名称，值包含：

- `safeLeftMm`
- `safeRightMm`
- `distributorNameFontSize`
- `orderContentFontSize`
- `lineHeight`
- `headerFontSize`
- `zoomFactor`
- `maxPrintableWidth`
- `updatedAt`

默认配置大致为左右各 12 mm、标题 18 px、订单 14 px、行高 24 px、表头 14 px、缩放 1.0、最大可打印宽度 200 mm。

### 5.2 当前优点

- 不依赖浏览器缓存，每次通过主进程文件系统读取；
- 不同打印机名称可以保存不同 Profile；
- 保存后立即重新读取并验证关键字段；
- 打印机选择和打印校准分开保存；
- 未配置打印机时能够回退到默认参数。

### 5.3 当前跨电脑风险

- 打印机名称可能因操作系统、共享方式、USB 端口或驱动重装而变化；
- 同名打印机在不同电脑上的驱动版本、默认纸型和不可打印区域可能不同；
- Profile 没有配置格式版本、设备指纹、操作系统、驱动版本和迁移状态；
- Profile 没有按全张/1/2/1/3 纸分别保存；
- Profile 没有按单列/双列模板分别保存；
- 没有自动备份、导入导出、校验和或损坏恢复；
- 多台同型号设备共用名称时，无法识别各自机械走纸差异；
- `printerProfile.js` 的默认 `maxPrintableWidth` 是 200 mm，而主进程清洗缺省值为 195 mm，虽然正常路径通常会传值，但属于需要固化的默认值差异。

## 6. 必须保护的现有适配逻辑

在实物回归通过前，下列逻辑进入“受保护清单”：

1. 六种模板及其客户配置名称；
2. 各模板的 30/60/12/26/5/12 行分页规则；
3. 240 mm 与 241 mm 的现存差异；
4. 279/140/93 mm 的纸高；
5. 210 mm 安全基准宽度；
6. 5–20 mm 人工边距范围；
7. 180–205 mm 最大可打印宽度范围；
8. 0.5–1.5 的校准缩放范围，以及个别模板保留的历史范围；
9. 驱动控制 DPI 的当前策略；
10. Epson 120 × 60 DPI 相关注释和系统分辨率能力；
11. `marginsType`、`printBackground`、`silent`、`deviceName` 的现有组合；
12. 隐藏打印窗口的 `zoomFactor = 1.0`；
13. 加载后额外等待和 10 秒超时；
14. 400/1500/4000 ms 走纸等待；
15. 逐页打印、最后一页才保存记录的业务语义；
16. 普通订单、国标订单、历史订单的不同保存路径；
17. MCP 只下发打印意图、最终复用桌面端模板和打印链路的流程。

“受保护”不代表永远不改，而是任何修改必须有明确设备矩阵、旧版对照和可回滚开关。

## 7. 主要兼容性风险

| 风险 | 表现 | 优先级 | 当前处理原则 |
|---|---|---:|---|
| 配置只按打印机名称识别 | 换电脑、改名或重装驱动后丢失适配 | P0（打印稳定性） | 先备份旧配置，再增加设备画像和迁移映射 |
| 240/241 mm 混用 | 某些模板或设备可能出现横向偏移 | P0（不可盲改） | 纳入实物基线，保留模板级覆盖 |
| 应用与驱动双重缩放 | 字体、边框和裁切位置漂移 | P1 | 校准时记录驱动设置，未来拆分 scaleX/scaleY |
| 默认边距与 CSS 无边距叠加 | 不同品牌的实际原点不同 | P1 | 建立可打印矩形和原点偏移校准 |
| DPI 兜底值不等于真实能力 | 300 DPI 可能不适合针式打印机 | P1 | 能力探测只作参考，用户确认值优先 |
| 逐页等待是固定时间 | 慢设备仍可能重叠，快设备效率低 | P1 | 先改为可配置设备参数，暂不删除默认值 |
| Electron 回调不等于物理完成 | 记录成功但纸张仍在打印或失败 | P1 | 区分“已提交”和“已完成/需人工确认” |
| 六模板复制大量布局逻辑 | 修一个模板可能遗漏其他模板 | P1 | 先快照测试，再逐模板抽取共享引擎 |
| 配置文件无版本和恢复 | 损坏后全部回退默认，现场结果变化 | P1 | 原子写入、备份、schemaVersion、自动恢复 |
| 旧打印函数参数不完全一致 | 普通/国标/批次行为可能漂移 | P1 | 建立调用清单，不在本轮直接统一 |

## 8. 从人工适配到生产级兼容的演进方案

### 阶段 A：冻结与建立实物基线

目标：任何后续重构都有可比较对象。

动作：

1. 给当前布局算法标记为 `legacy-v1`，保持现有输出；
2. 导出现场 `printer-config.json` 和 `printer-profiles.json`；
3. 记录电脑、操作系统、Electron 版本、打印机品牌/型号、连接方式、驱动名称/版本、驱动纸型、驱动缩放和 DPI；
4. 六种模板分别准备空数据、临界行数、刚好一页、多一行、多页和超长商品名称样本；
5. 在纸张上打印毫米刻度、四边框、中心线、二维码和中文字体样张；
6. 记录左右/上下偏移、横向比例、纵向走纸比例、裁切、重叠、页数和打印记录；
7. 保存打印 HTML、配置快照和实物照片，形成“黄金样本”。

验收：

- 六种模板在现有 Epson 设备上的输出不低于当前现场基线；
- 页数、分页点、最后一页保存记录和连续打印顺序完全一致；
- 每个测试结果能够追溯到具体电脑、驱动、设备和配置。

### 阶段 B：引入分层配置，但继续使用旧算法

目标：解决多品牌、多电脑和驱动变化的识别问题，不改变布局公式。

建议配置结构：

```text
schemaVersion
computerId
printerIdentity
  systemName
  displayName
  brand
  model
  connection
driverIdentity
  os
  driverName
  driverVersion
  capabilityHash
paperProfiles
  full
  half
  third
templateOverrides
  ApplyPanel
  ApplyFiftyPanel
  ...
legacyProfile
timing
  interPageDelayMs
  renderDelayMs
```

配置优先级建议：

```text
本次临时覆盖
> 当前电脑 + 当前设备 + 当前驱动 + 当前纸型 + 当前模板
> 当前设备 + 当前纸型
> 品牌/型号推荐值
> 旧版“打印机名称”Profile
> 当前代码默认值
```

兼容策略：

- 首次升级只读入旧 Profile，不改其值；
- 找不到新配置时继续走 `legacy-v1`；
- 新配置保存成功且完成实物验证后，才切换单台设备；
- 旧 Profile 保留备份和一键回退；
- 240/241 mm、固定等待等历史值允许作为模板级覆盖保存。

### 阶段 C：建立每台电脑的打印校准向导

目标：把“反复改参数试纸”变成可记录、可复现的标准步骤。

校准向导建议按纸型执行：

1. 选择打印机，展示品牌、型号、驱动和系统纸型；
2. 锁定驱动缩放为 100% 或记录当前实际设置；
3. 打印四边框和 100 mm 横纵标尺；
4. 用户输入实测横向长度、纵向长度、左边偏移和上边偏移；
5. 系统计算 `scaleX`、`scaleY`、`offsetLeftMm`、`offsetTopMm`；
6. 打印第二张验证页；
7. 分别验证全张、1/2、1/3；
8. 最后验证单列、双列和连续多页走纸；
9. 保存配置、现场照片、操作人和更新时间。

建议不要继续用一个 `zoomFactor` 同时修正所有问题。新模型可以计算独立参数，但旧模板在迁移完成前仍使用旧值。

### 阶段 D：建立品牌适配层

目标：新增 Epson 之外的设备时有稳定入口。

品牌适配层不应写成大量 `if (printerName.includes(...))`，而应包含：

- 设备能力探测；
- 推荐 DPI 和驱动选项；
- 默认可打印矩形；
- 默认纸型映射；
- 默认逐页等待；
- 已验证的驱动版本范围；
- 不支持能力和现场注意事项。

品牌默认值只作为首次校准的起点，最终以“当前电脑 + 当前驱动 + 当前设备”的实测值为准。

### 阶段 E：逐模板迁移到共享布局引擎

目标：标准化纸型和计算逻辑，同时保持实物输出。

推荐顺序：

1. 先抽取纯数据定义，不改 HTML/CSS；
2. 抽取 `PaperSpec`：全张、1/2、1/3 的标准标识和默认尺寸；
3. 抽取 Profile 解析和配置优先级；
4. 抽取分页计算；
5. 抽取打印任务编排、回执和保存记录；
6. 最后才抽取共享 CSS/布局计算；
7. 每次只迁移一个模板，并保留 `legacy-v1` 开关；
8. Epson 实物对照通过后，再在另一品牌上验证；
9. 六种模板全部通过后，才考虑删除重复旧逻辑。

禁止一次性把六种模板合并成一个通用组件。它会把已经稳定的现场差异同时带入一个不可定位的回归范围。

## 9. 纸型标准化建议

“标准化”应标准化配置模型和测试方法，而不是立即强制所有模板使用完全相同的数字。

建议的业务纸型注册表：

| 标准标识 | 业务名称 | 默认尺寸 | 允许设备/模板覆盖 |
|---|---|---:|---|
| `FULL` | 全张纸 | 240 × 279 mm | 是 |
| `HALF` | 1/2 纸 | 240 × 140 mm | 是 |
| `THIRD` | 1/3 纸 | 240 × 93 mm | 是，兼容现有 241 mm 路径 |

每个纸型还应保存：

- 驱动中的真实 PageSize 名称；
- 物理纸宽、纸高；
- 驱动可打印矩形；
- 左上原点偏移；
- 横向和纵向比例；
- 进纸方向；
- 连续纸/单页纸；
- 走纸等待；
- 已验证模板集合。

## 10. 可配置边距建议

当前仅有左右安全边距。生产级模型应逐步支持：

- `contentLeftMm/contentRightMm`：业务内容留白；
- `contentTopMm/contentBottomMm`：业务内容留白；
- `deviceOffsetX/deviceOffsetY`：设备物理原点补偿；
- `unprintableLeft/Right/Top/Bottom`：驱动不可打印区；
- `templateOverride`：个别模板的现场覆盖。

界面上仍可向普通用户展示“左、右、上、下边距”，高级设备参数放在管理员校准模式中，避免现场用户误改驱动级补偿。

边距配置必须满足：

- 数值范围校验；
- 修改前自动备份；
- 显示影响的纸型和模板；
- 打印验证页后才能标记为“已验证”；
- 一键回退到上一版本或 `legacy-v1`。

## 11. Phase 1 的打印保护验收标准

Phase 1 安全整改完成后，至少满足：

1. 六种模板文件的正式打印 HTML 和关键 CSS 未被无意修改；
2. 240/241、279/140/93、分页行数和逐页等待值保持不变；
3. 主进程正式打印选项保持现有语义；
4. 默认打印机与 Profile 的旧文件能够原样读取；
5. 普通订单、国标订单、批次打印和历史订单入口仍可调用；
6. 多页打印仍只在最后一页保存一次打印记录；
7. MCP 打印继续进入同一套 Vue 模板和 Electron 打印流程；
8. OCR 功能不依赖本轮打印调整，不发生接口回归；
9. preload 收窄后，所有已登记打印方法仍具备相同参数和返回值；
10. 系统命令安全改造若未完成真实设备验证，则保留原实现并隔离风险，不直接切换；
11. Vue 生产构建成功；
12. 至少完成非设备自动回归；正式发布前补齐 Epson 实物回归。

## 12. 推荐实施顺序

1. 冻结受保护参数并建立本报告；
2. 备份现场打印配置，形成 Epson 黄金样本；
3. Phase 1 只收窄 preload、IPC、MCP 和动态脚本边界；
4. 保持现有打印参数、模板和任务语义；
5. 增加打印契约测试和 HTML 快照测试；
6. 把系统命令改为无 shell 参数调用，并进行真实设备验证；
7. 引入带版本的分层 Profile，同时保留旧配置回退；
8. 上线校准向导；
9. 新增其他品牌设备矩阵；
10. 最后逐模板迁移共享布局引擎。

## 13. 最终回答

要从当前人工适配逐步演进为多品牌、多电脑、纸型标准化和可配置边距，正确路线不是删除现场参数，而是：

```text
保留并命名现有 legacy-v1
→ 建立 Epson 实物黄金基线
→ 用设备/电脑/驱动/纸型/模板分层保存配置
→ 用校准页量化比例、原点和不可打印区
→ 为不同品牌提供“推荐初值 + 实测覆盖”
→ 逐模板迁移共享布局引擎
→ 全程保留旧模式回退
```

这条路线能够把当前依赖经验的人工适配，逐步变成可识别、可配置、可验证、可迁移、可回滚的生产级打印体系，同时最大限度保护已经稳定运行的现场业务。
