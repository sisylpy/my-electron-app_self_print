# Slice 3.5 真实业务闭环验证报告

> 项目：农心乐配送商系统  
> Electron 项目：`ai-priter-electic`  
> 验证日期：2026-07-30  
> 验证方式：线上服务与数据库只读核验、测试配送商只读接口核验、代码自动化回归  
> 结论：**No-Go；Slice 3.5 真实业务闭环尚未通过**

## 1. 执行结论

Slice 3.0 的 Electron 页面、写接口调用约束、安全处理和受保护打印流程通过了代码级回归，但当前线上环境不具备真实闭环验证条件：

1. 当前线上 WAR 没有部署 `/api/desktop-dispatch/v1/**` 认证与写接口。
2. 生产库和测试库都没有执行 Slice 2.0 安全底座迁移。
3. 已识别的测试配送商存在待分配站点，但没有可用司机。
4. 因此 Boss 扫码授权、Electron 登录、分配司机、确认派单、Boss 状态回查、司机状态回查均无法安全执行。

本次没有部署 WAR、没有执行数据库迁移、没有伪造登录身份，也没有对任何订单、路线、司机或配送状态进行写入。

## 2. 环境核验

### 2.1 服务部署状态

线上地址：

`https://grainservice.club:8443/nongxinle`

只读核验结果：

| 检查项 | 结果 | 判定 |
|---|---:|---|
| 旧沙盘查询接口 | HTTP 200 | 旧只读能力在线 |
| `/api/desktop-dispatch/v1/capabilities` | HTTP 404 | 新桌面接口未部署 |
| 线上 WAR 修改时间 | 2026-07-29 13:24:53 +08:00 | 已记录 |
| WAR 内 `DesktopDispatchAuthController` | 不存在 | 未部署 |
| WAR 内 `DesktopDispatchCommandController` | 不存在 | 未部署 |
| WAR 内桌面调度 security 类 | 不存在 | 未部署 |

未登录访问 `capabilities` 的正确结果应为 401。当前返回 404，说明请求尚未进入桌面调度认证链。

### 2.2 数据库状态

服务器 MySQL 版本：`8.0.27`。

对 `nongxinletest` 和 `nongxinle` 的 `information_schema` 进行了只读核验，两个库结果一致：

| 结构 | 测试库 | 生产库 |
|---|---:|---:|
| `nx_dis_dispatch_session` | 缺失 | 缺失 |
| `nx_dis_dispatch_login_challenge` | 缺失 | 缺失 |
| `nx_dis_dispatch_idempotency` | 缺失 | 缺失 |
| `nx_dis_dispatch_operation_log` | 缺失 | 缺失 |
| `nx_dis_driver_route.nx_ddr_version` | 缺失 | 缺失 |
| `nx_dis_shipment_task.nx_dst_version` | 缺失 | 缺失 |
| `uk_ddr_plan_driver` | 缺失 | 缺失 |

生产库迁移前风险查询：

- `nx_dis_driver_route` 估算 5 行。
- `nx_dis_shipment_task` 估算 10 行。
- 重复“路线计划 + 司机”组合为 0 组。
- 路线计划或司机为空的路线记录为 0 行。

结论：迁移前数据条件正常，但迁移尚未执行，Token、幂等、版本和审计不可能真实落库。

### 2.3 测试配送商数据

通过名称只读筛选到以下可作为候选的测试配送商：

| disId | 名称 | 账号情况 |
|---:|---|---|
| 166 | 测试配送商贸中心 | 测试库和生产库均有管理员账号 |
| 169 | 169 测试 | 测试库和生产库均有管理员账号 |

对旧只读沙盘接口进行早班、午班和临时批次查询：

| disId | 查询结果 | 未分配站点 | 可用司机 |
|---:|---:|---:|---:|
| 166 | HTTP 200，业务码 0 | 1 | 0 |
| 169 | HTTP 200，业务码 0 | 1 | 0 |

未读取或输出 openid、手机号、Token 哈希、客户名称和地址等敏感数据。

当前测试数据不能完成司机选择和派单。继续前需要业务方确认其中一个测试配送商，并让至少一个测试司机完成登录或上岗，使其进入服务端可用司机列表。

## 3. 真实业务流程验证

| 流程 | 结果 | 证据或阻断 |
|---|---|---|
| Boss 扫码授权 | 阻断 | 登录挑战接口未部署，挑战表未创建 |
| Electron 登录 | 阻断 | 无法创建或换取桌面 Token |
| 读取真实订单 | 部分通过 | 测试配送商旧只读接口返回真实快照；新 Token 身份绑定未验证 |
| 选择司机 | 阻断 | 两个测试配送商可用司机均为 0 |
| 分配司机 | 未执行 | 写接口未部署，版本字段和审计表缺失 |
| 确认派单 | 未执行 | 写接口未部署，版本字段和幂等表缺失 |
| 检查 Boss 端状态 | 未执行 | 前序写入未发生 |
| 检查司机端状态 | 未执行 | 前序写入未发生，且没有可用测试司机 |

真实闭环验收结果：**未通过**。该结果是环境未就绪导致的 No-Go，不是用静态测试替代真实验收。

## 4. 异常场景验证

| 场景 | 线上实测 | 代码级验证 | 结论 |
|---|---|---|---|
| Token 过期 | 未执行 | Electron 已实现重新扫码提示；服务端存在过期校验代码 | 待实测 |
| 401 | 新接口当前返回 404 | Electron 状态处理和服务端拦截器测试通过 | 线上未通过 |
| 403 | 未执行 | 权限拦截器直接测试通过 | 待实测 |
| 两台电脑同时操作 409 | 未执行 | 幂等与冲突合同检查通过 | 待双机实测 |
| `taskVersion` 冲突 | 未执行 | 请求合同要求版本，服务端冲突分支存在 | 待实测 |
| `routeVersion` 冲突 | 未执行 | 请求合同要求版本，服务端冲突分支存在 | 待实测 |

两台电脑并发测试必须在迁移和测试 WAR 部署后使用两个独立 Electron 设备会话完成，不得通过直接改库模拟。

## 5. 数据库落库验证

| 验收项 | 当前结果 |
|---|---|
| 新增认证表 | 未落库 |
| Token / 会话表 | 未落库 |
| 登录挑战表 | 未落库 |
| 幂等表 | 未落库 |
| 路线版本字段 | 未落库 |
| 任务版本字段 | 未落库 |
| 操作审计记录 | 无法产生 |

数据库落库验收：**失败**。

已补充正式迁移方案：

`nongxinle-server/docs/route-dispatch/Slice3.5数据库正式迁移方案.md`

已将迁移 SQL 调整为可重复执行、带重复数据保护和验收计数的版本：

`nongxinle-server/docs/sql/patches/upgrade_nx_dis_dispatch_slice2_0_security.sql`

本次没有执行迁移。

## 6. 自动化回归

### 6.1 Electron

执行 `npm run test:phase2-slice3`：

- Slice 0 架构隔离：通过。
- Slice 3.0 写合同：通过。
- Electron 安全检查：通过。
- 打印契约：通过。
- Vue 生产构建：通过，597 个模块完成构建。

构建仍有既有的大包体积警告，但不影响本次功能正确性判断。

### 6.2 服务端

Maven 项目默认配置会跳过 Surefire 测试，因此另外直接运行 JUnit 4：

- `DesktopDispatchAuthInterceptorTest`
- `DesktopDispatchPermissionServiceTest`
- `DesktopDispatchIdempotencyServiceTest`

结果：`OK (11 tests)`。

这些结果证明代码分支和合同存在，不证明线上环境已部署，也不替代真实扫码、写入、并发和多端状态验证。

## 7. 受保护模块影响

本次 Slice 3.5 没有修改：

- print 模块
- Bills
- OCR
- MCP
- 路线编辑功能
- nongxinle-server 业务状态机

打印契约和 MCP 安全回归全部通过。

## 8. 已补充内容

本次仅补充验证与迁移制品：

1. 把 Slice 2.0 迁移 SQL 的裸 `ALTER` 改为基于 `information_schema` 的可重复执行过程。
2. 在增加唯一索引前检查重复数据。
3. 检测同名但定义错误的索引并中止，不自动覆盖。
4. 增加迁移后的 `4、1、1、1` 结构验收计数。
5. 新增测试库优先、生产库后置、代码回退但结构保留的正式迁移方案。

没有对线上服务或数据库执行这些变更。

## 9. 下一次 Slice 3.5 复验准入条件

以下条件全部满足后再进行真实闭环：

1. 在 `nongxinletest` 执行迁移两次，两次均成功且结构计数均为 `4、1、1、1`。
2. 将包含 `/api/desktop-dispatch/v1/**` 的候选 WAR 部署到测试环境。
3. 未登录访问 `capabilities` 返回 401，不再返回 404。
4. 业务方确认测试配送商 166 或 169。
5. 准备一个 Boss 测试账号、至少一个已上岗的司机测试账号和一个明确可回滚的测试订单。
6. 准备两台 Electron 设备或两个独立设备身份，用于并发 409 验证。
7. 测试通过后，再按正式迁移方案进入生产维护窗口。

## 10. 最终判定

| 维度 | 判定 |
|---|---|
| Slice 3.0 代码与构建 | 通过 |
| 打印 / Bills / OCR / MCP 隔离 | 通过 |
| 线上桌面 v1 接口 | 未部署 |
| 数据库安全底座 | 未迁移 |
| 测试司机条件 | 不满足 |
| 真实业务闭环 | 未通过 |
| 是否可作为可运行产品开放 | **否，当前 No-Go** |

建议下一动作不是继续开发功能，而是先在 `nongxinletest` 完成迁移、部署候选 WAR、补齐测试司机，再重新执行同一份 Slice 3.5 验收清单。
